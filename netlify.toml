[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# SPA fallback - wszystkie ścieżki obsługuje React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
