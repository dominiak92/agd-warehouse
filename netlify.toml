[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # PWA (workbox) wymaga Node >= 18.19 / 20 — używamy 20 na produkcji
  NODE_VERSION = "20"

# SPA fallback - wszystkie ścieżki obsługuje React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
